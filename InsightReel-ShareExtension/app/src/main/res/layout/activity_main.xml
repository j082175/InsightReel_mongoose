<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- 상단: 작은 설정/테스트 버튼들 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="24dp">

        <Button
            android:id="@+id/settingsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚙️ 설정"
            android:textSize="12sp"
            android:padding="8dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/button_test_background" />

        <Button
            android:id="@+id/testButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🧪 연결 테스트"
            android:textSize="12sp"
            android:padding="8dp"
            android:background="@drawable/button_save_background"
            android:textColor="#FFFFFF" />

    </LinearLayout>

    <!-- 중앙: 클립보드 버튼만 위한 공간 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- 클립보드 버튼이 동적으로 추가될 공간 -->

    </LinearLayout>

    <!-- 하단: 작은 설명 텍스트 -->
    <TextView
        android:id="@+id/descriptionText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="YouTube/Instagram/TikTok에서 '공유' → 'InsightReel Share' 선택하세요!\n\n또는 아래에 링크를 직접 입력할 수도 있습니다."
        android:textSize="12sp"
        android:textAlignment="center"
        android:layout_marginTop="16dp" />

</LinearLayout>