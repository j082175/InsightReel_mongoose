{
  "name": "video-auto-saver-extension",
  "version": "1.0.0",
  "description": "영상 자동저장 분석기 Chrome Extension",
  "private": true,
  "scripts": {
    "build": "webpack --mode=production",
    "dev": "webpack --mode=development --watch",
    "clean": "rimraf dist",
    "build:clean": "npm run clean && npm run build"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0",
    "dotenv": "^16.3.0",
    "rimraf": "^5.0.0",
    "@babel/core": "^7.22.0",
    "@babel/preset-env": "^7.22.0",
    "babel-loader": "^9.1.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}